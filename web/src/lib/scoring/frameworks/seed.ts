import type { Framework } from "./types";

/**
 * SEED EVALUATION FRAMEWORK
 *
 * TODO: paste rubric here. Until sections is populated, the framework
 * selector falls back to PRE_SEED_FRAMEWORK.
 */
export const SEED_FRAMEWORK: Framework = {
  stage: "Seed",
  philosophy: "(pending — paste seed rubric)",
  sections: [],
};
